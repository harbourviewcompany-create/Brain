# Agent Run Reports

Each agent run must create a report using `docs/agent-control/AGENT_HANDOFF_TEMPLATE.md`.

Required filename:

```text
reports/agent-runs/<ticket_id>-<agent>-<YYYYMMDD-HHMM>.md
```

Required status: GO or HOLD.
