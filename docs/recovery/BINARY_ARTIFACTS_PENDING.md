# Binary Artifacts Pending

Status: preservation blocker.

The current recovery package zip was generated in the working environment as:

`/mnt/data/brain_source_recovery_package.zip`

Expected repository path:

`artifacts/brain_source_recovery_package.zip`

Expected SHA-256:

`adb1bf1cfdc59106c6113c19a2a30c7c933a99ca13c902ccb086513a6b4419b0`

Expected size:

`17,533` bytes

The text contents of the package have been preserved under `docs/recovery/`. The binary zip itself remains pending if the available write path cannot commit binary blobs safely.

GO/HOLD: preservation is HOLD for binary completeness until the zip is committed as an actual repository blob or explicitly superseded.
