# Note on Incomplete Binary Preservation

This preservation branch commits the text recovery artifacts under `docs/recovery/` and records the generated recovery zip checksum.

The generated binary zip itself is not included in this branch. The intended binary path remains:

`artifacts/brain_source_recovery_package.zip`

Expected SHA-256:

`adb1bf1cfdc59106c6113c19a2a30c7c933a99ca13c902ccb086513a6b4419b0`

Expected size:

`17,533` bytes

The branch should not be treated as complete binary preservation until that artifact is committed as an actual repository blob.
